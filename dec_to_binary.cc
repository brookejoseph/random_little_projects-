#include <iostream>
#include <sstream>

using namespace std;
int main(int argc, char *argv[])
{
    stringstream number;
    int value;
    number >> value;
    while (value / 2 != 0)
    {
        if (value % 2 == 0)
        {
        }
    }
    return 0;
}